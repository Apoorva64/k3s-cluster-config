apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: nominatim
  namespace: nominatim
spec:
  interval: 5m
  url: https://github.com/Apoorva64/helm-charts

  ref:
    branch: master