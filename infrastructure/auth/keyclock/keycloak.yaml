apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: main-keycloak
spec:
  instances: 3
  db:
    vendor: mariadb
    host: mariadb-galera
    database: keycloak
    usernameSecret:
      name: db-username
      key: username
    passwordSecret:
      name: keycloak-db-secret
      key: password
  hostname:
    hostname: keycloak.auth.apoorva64.com
